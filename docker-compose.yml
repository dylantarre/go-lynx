version: '3'

services:
  go-lynx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: go-lynx
    restart: unless-stopped
    ports:
      - "3500:3500"
    env_file:
      - .env
    volumes:
      - music-data:/music

volumes:
  music-data:
    name: music-data
